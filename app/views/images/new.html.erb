<h1>add the URL of your favorite image! </h1>

<%= form_for(@image, url: images_path) do |form| %>
    <p>
      <%= form.text_field :imagelink %>
    </p>

    <p>
      <%= form.submit "Add image" %>
    </p>
<% end %>

